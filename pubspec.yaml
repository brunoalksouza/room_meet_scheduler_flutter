name: room_meet_scheduler_flutter
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  table_calendar: ^3.0.9
  flutter_localizations:
    sdk: flutter
  flutter_datetime_picker: ^1.5.1
  mongo_dart: ^0.9.1
  http: ^1.1.0
  shared_preferences: ^2.2.1
  jwt_decoder: ^2.0.1
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
